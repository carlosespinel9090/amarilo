# Copiar a `.env` para desarrollo local (Vite).
VITE_API_BASE_URL=https://stage-amarilo.ddev.site/api
VITE_GTM_ID=GTM-XXXXXXX
VITE_JWT_TOKEN=

# Zoho SalesIQ — widget code (wc=) from Installation → Website
VITE_SALESIQ_WIDGET_CODE=

# Producción / Cloudflare: ver `.env.production` (Pantheon Dev API).
